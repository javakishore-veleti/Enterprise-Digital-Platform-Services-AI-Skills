# spring-boot-web

Enterprise Digital Platform AI capability family for Servlet-stack REST APIs, MVC controllers, request handling, validation, exception handling, filters, interceptors, and HTTP behavior.

Public skills created inside this folder must use the `spring-boot-web-` prefix when the capability name alone could be ambiguous. Cross-stack concerns should route to the owning technology family rather than duplicate platform-specific knowledge.
