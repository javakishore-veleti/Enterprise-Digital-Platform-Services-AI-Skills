# spring-boot-grpc

Enterprise Digital Platform AI capability family for Spring gRPC services and clients, protobuf contracts, streaming, interceptors, security, deadlines, retries, and observability.

Public skills created inside this folder must use the `spring-boot-grpc-` prefix when the capability name alone could be ambiguous. Cross-stack concerns should route to the owning technology family rather than duplicate platform-specific knowledge.
