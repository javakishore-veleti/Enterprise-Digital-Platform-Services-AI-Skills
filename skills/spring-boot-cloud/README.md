# spring-boot-cloud

Enterprise Digital Platform AI capability family for Spring Cloud configuration, discovery, clients, gateways, distributed-system patterns, cloud-native integration, and service-to-service behavior.

Public skills created inside this folder must use the `spring-boot-cloud-` prefix when the capability name alone could be ambiguous. Cross-stack concerns should route to the owning technology family rather than duplicate platform-specific knowledge.
