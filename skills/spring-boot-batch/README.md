# spring-boot-batch

Enterprise Digital Platform AI capability family for Spring Batch jobs, steps, readers, processors, writers, restartability, partitioning, checkpoints, and large-scale batch processing.

Public skills created inside this folder must use the `spring-boot-batch-` prefix when the capability name alone could be ambiguous. Cross-stack concerns should route to the owning technology family rather than duplicate platform-specific knowledge.
