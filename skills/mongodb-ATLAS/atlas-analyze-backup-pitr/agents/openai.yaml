interface:
  display_name: "MongoDB Atlas Analyze Backup PITR"
  short_description: "Analyze MongoDB Atlas managed backup, continuous cloud backup, point-in-time restore, snapshot policy, retention, r"
