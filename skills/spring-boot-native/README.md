# spring-boot-native

Enterprise Digital Platform AI capability family for AOT processing, GraalVM native images, startup/memory optimization, reflection/resource hints, buildpacks, and native runtime diagnostics.

Public skills created inside this folder must use the `spring-boot-native-` prefix when the capability name alone could be ambiguous. Cross-stack concerns should route to the owning technology family rather than duplicate platform-specific knowledge.
