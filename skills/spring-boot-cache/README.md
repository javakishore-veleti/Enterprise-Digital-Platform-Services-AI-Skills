# spring-boot-cache

Enterprise Digital Platform AI capability family for Spring Cache abstraction, Redis-backed caching, cache-aside integration, TTL/invalidation strategy, serialization, and cache observability.

Public skills created inside this folder must use the `spring-boot-cache-` prefix when the capability name alone could be ambiguous. Cross-stack concerns should route to the owning technology family rather than duplicate platform-specific knowledge.
